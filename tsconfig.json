{
  // Nuxt generates .nuxt/tsconfig.json during `nuxt prepare` (runs via postinstall).
  // All compiler options, path aliases, and type references are managed there.
  "extends": "./.nuxt/tsconfig.json",
  "compilerOptions": {
    "skipLibCheck": true
  }
}
